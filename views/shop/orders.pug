extends ../layouts/main-layout.pug

block content
  main
    if orders.length
      ul
        each order in orders
          li
            h1  #{order.id}
            ul
              each product in order.products
                li
                  div
                    h2  #{product.title} (#{product.orderItem.quantity})
    else
      h1  Nothing there!